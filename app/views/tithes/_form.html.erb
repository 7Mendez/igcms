<%= form_with(model: tithe) do |form| %>
  <% if tithe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tithe.errors.count, "error") %> prohibited this tithe from being saved:</h2>

      <ul>
        <% tithe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :note, style: "display: block" %>
    <%= form.textarea :note %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.collection_select :member_id, Member.all, :id, :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
